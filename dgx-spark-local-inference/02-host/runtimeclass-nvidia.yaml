# k3s registers the `nvidia` containerd runtime when /usr/bin/nvidia-container-runtime exists
# (40-k3s.sh in this folder); GPU pods set runtimeClassName: nvidia. Apply once, after k3s.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
